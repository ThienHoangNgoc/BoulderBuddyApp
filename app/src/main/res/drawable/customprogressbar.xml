<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="10dip" />
            <gradient
                android:angle="270"
                android:centerColor="@color/colorProgressbarBackground"
                android:centerY="1.0"
                android:endColor="@color/colorProgressbarBackground"
                android:startColor="@color/colorProgressbarBackground" />

            <stroke
                android:width="1dp"
                android:color="@color/colorProgressbarBackground" />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress"
        android:top="1dp"
        android:bottom="1dp"
        android:left="1dp"
        android:right="1dp">

        <scale android:scaleWidth="100%">
            <shape>
                <corners android:radius="10dip" />
                <gradient
                    android:angle="270"
                    android:centerColor="@color/colorProgressbar"
                    android:centerY="1.0"
                    android:endColor="@color/colorProgressbar"
                    android:startColor="@color/colorProgressbar" />
            </shape>
        </scale>
    </item>
</layer-list>